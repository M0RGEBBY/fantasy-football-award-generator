{
  "name": "fantasy-football-award-generator",
  "version": "1.0.0",
  // ... other properties (dependencies, etc.) ...

  **"scripts"**: {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "postbuild": "tailwindcss -i ./src/style.css -o ./dist/style.css --minify"
  },

  "devDependencies": {
    "@tailwindcss/cli": "^4.1.17",
    "autoprefixer": "^10.4.22",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.17"
  }
}
